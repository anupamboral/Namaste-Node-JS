var a = 10;
var b = 20;

console.log(a + b);
//*to run app.js then in the terminal we just need to write "node app.js" then enter and it will execute our code. As Node.js already contains V8 inside it.
console.log(global);
